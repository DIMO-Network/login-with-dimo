export enum Environment {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  LOCAL = 'local',
}

export enum DimoSDKModes {
  POPUP = 'popup',
  EMBED = 'embed',
  REDIRECT = 'redirect',
}
